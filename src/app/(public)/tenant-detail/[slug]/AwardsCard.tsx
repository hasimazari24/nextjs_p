import  * as React from "react";
import { Box, Stack, VStack } from "@chakra-ui/react";

interface AwardsCardProps {
    logo: string;
    title: string;
    awards: string;
}

const CardAwards = ({logo, title, awards}: AwardsCardProps) => {

    return (
        <Box>
            <VStack>

            </VStack>
        </Box>
    );
 };